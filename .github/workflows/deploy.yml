name: Deploy Backend to Swarm

on:
  pull_request:
    branches:
      - main  # Запускать CI/CD при PR в release-ветку

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Клонируем код репозитория
        uses: actions/checkout@v4

      - name: Готовим переменную тега с датой и временем
        run: echo "IMAGE_TAG=$(date +'%d.%m.%Y_%H.%M.%S')" >> $GITHUB_ENV

      - name: Устанавливаем переменную окружения IMAGE_URL
        run: echo "IMAGE_URL=${{ secrets.REGISTRY_URL }}/${{ secrets.APP_NAME }}:${{ env.IMAGE_TAG }}" >> $GITHUB_ENV

      - name: Устанавливаем переменную окружения SERVICE_NAME
        run: echo "SERVICE_NAME=${{ secrets.APP_NAME }}_stack__backend" >> $GITHUB_ENV

      - name: Собираем и пушим Docker-образ приложения
        run: |
                docker buildx build --platform linux/amd64 \
                  --build-arg DATABASE_URL=${{ secrets.DATABASE_URL }} \
                  -t ${{ env.IMAGE_URL }} \
                  -f Dockerfile . --push

      # 🔹 2. Логинимся в Docker Registry
#      - name: Login to Docker Registry
#        run: echo "${{ secrets.DOCKER_REGISTRY_PASSWORD }}" | docker login registry.good-buket-tech.ru -u "${{ secrets.DOCKER_REGISTRY_USER }}" --password-stdin

      # 🔹 3. Проверяем, есть ли изменения в миграциях
#      - name: Check for migration changes
#        id: check_migrations
#        run: |
#          if git diff --name-only origin/main..HEAD | grep -E 'migrations|drizzle.config.ts'; then
#            echo "MIGRATIONS_NEEDED=true" >> $GITHUB_ENV
#          else
#            echo "MIGRATIONS_NEEDED=false" >> $GITHUB_ENV
#          fi  REGISTRY_URL APP_NAME



      # 🔹 5. Подключаемся по SSH и запускаем миграции в Swarm (если миграции нужны)
      - name: Подключаемся по SSH и обновляем сервис в Swarm
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker service update --image ${{ env.IMAGE_URL }} ${{ env.SERVICE_NAME }}
            docker service ps ${{ env.SERVICE_NAME }}
#            docker stack deploy -c docker-compose.migrations.yml migrations_stack
#            sleep 10  # Ожидание завершения миграций
#            docker service logs migrations_stack_migrator
#            docker stack rm migrations_stack  # Удаляем контейнер после выполнения

      # 🔹 7. Подключаемся по SSH и обновляем сервис в Swarm
#      - name: Deploy Backend to Swarm
#        uses: appleboy/ssh-action@v0.1.10
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USER }}
#          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          script: |
#            docker service update --image registry.good-buket-tech.ru/backend:${{ github.run_number }} back2_backend
#            docker service ps backend_backend
